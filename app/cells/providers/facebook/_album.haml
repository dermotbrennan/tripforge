%li.photo_album
  .image= link_to image_tag("https://graph.facebook.com/#{album['id']}/picture?access_token=#{@access_token}"), '#'+album["id"]
  %h2
    = link_to truncate(album['name'], :length => 15), '#'+album['id']
    == (#{album['count']})