%h2= event.name
.description= nl2br(event.description)
%p
  == #{event.started_at} - #{event.ended_at} in #{event.location}
.items
  - if !event.items.empty?
    %ul= render :partial => 'items/mini_item', :collection => event.items